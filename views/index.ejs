<!DOCTYPE html>
<html lang="en">
<head> 
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo App</title>
    <link href="https://fonts.googleapis.com/css?family=Lato:100" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
    <style>
        body {
            background: #14848c;
            color: #ffffff;
            font-family: 'Lato', sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 80%;
            margin: 0 auto;
            text-align: center;
            padding-top: 50px;
        }
        .container img {
            max-width: 20%;
            max-height: 110%;
        }
        .container h2 {
            margin-bottom: 20px;
        }
        .container input[type="text"] {
            width: 60%;
            padding: 10px;
            margin-bottom: 10px;
            border: none;
            border-radius: 5px;
        }
        .container .fgx-button {
            background-color: #ffffff;
            color: #14848c;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .container .fgx-button:hover {
            background-color: #eaeaea;
        }
        .container footer {
            margin-top: 50px;
        }
        .container footer p {
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="https://s3.amazonaws.com/devopsfarm.in/devopsfarm-logo-1500x1500.png" alt="To Do App Logo"> 
        <h2>ToDo App - Designed by Kloudlab Team.</h2>

        <form action="/addtask" method="POST">
            <input type="text" name="newtask" placeholder="Add new task">
            <button class="fgx-button" type="submit">Add Task</button>
        </form>

        <h2>MY Added Tasks</h2>
        <ul>
            <% for(var i = 0; i < task.length; i++){ %>
                <input type="checkbox" name="check" value="<%= task[i] %>"><%= task[i] %>
            <% } %>
        </ul>
<br>
        <form action="/removetask" method="POST">
            <button class="fgx-button" type="submit">Remove</button>
        </form>

        <h2>Completed Tasks</h2>
        <ul>
            <% for(var i = 0; i < complete.length; i++){ %>
                <input type="checkbox" checked disabled><%= complete[i] %>
            <% } %>
        </ul>
        <footer>
            <p><strong>Created With Love ❤ And Effort By: Team Kalkitech 2024 </strong></p>
        </footer>
    </div>
</body>
</html>

